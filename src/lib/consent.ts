/**
 * Wording shown next to consent checkboxes. Shared by the checkout form and
 * the server, so the text stored in a ConsentRecord is exactly the text the
 * shopper saw: under the DPDP Act, consent has to be specific and provable.
 * Changing the wording changes what future records say; old records keep theirs.
 */
export const MARKETING_CONSENT_TEXT = "Send me occasional offers and new product news. You can stop this at any time.";
